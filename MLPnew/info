train_inputs:这是在训练期间传递给模型的输入图像。每个图像是一个张量，通常具有形状 对于AlexNet模型输入，每个图像会被调整为 [3, 224, 224] 的形状，其中3代表RGB通道，224x224是图像的高和宽。
train_outputs:这是模型在训练期间生成的输出结果。输出结果是经过模型预测后的张量，通常表示每个输入图像属于各个类别的概率分布。
val_inputs
val_outputs